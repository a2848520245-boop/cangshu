# 仓鼠 M1 代码仓入口

先读取本仓上一级的 `../AGENTS.md`（本机：`E:/AgentWork/CangShu/AGENTS.md`），再按任务读取笔记库规范。父目录位于 Git 根之外，不能假定客户端会自动加载它。

若在独立克隆中上级规则不存在，从 `E:/詩/Documents/NOTE/obsidian-kb-starter/10-常用/仓鼠/仓鼠项目-总览.md` 查规范；两处均不可访问时只做不依赖缺失决策的工作，报告缺口。

- 先核验 HEAD 和工作树，不回退他人修改；冻结原型与数据目录不在本仓修改范围。
- M1 范围、T1–T6、数据模型以批准规范为准。业务字节不入库；删除是引用删除与延迟回收。
- 任务完成须有当前提交与当前文档基线的实际验收证据；构建成功不等于端到端通过。
- 文档治理工具用法见 `tools/doc_governance/README.md`，测试输出不代表业务实现已完成。
